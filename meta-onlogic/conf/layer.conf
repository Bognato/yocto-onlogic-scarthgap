# Layer configuration for meta-onlogic
BBPATH := "${BBPATH}:${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-onlogic"
BBFILE_PRIORITY_meta-onlogic = "6"
LAYERVERSION_meta-onlogic = "1"